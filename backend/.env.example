# Deprecated: use ../.env.example as the only authoritative template.
# Backend local run now reads env vars from repository root .env.
# Keep this file only as a compatibility note for existing docs/scripts.
